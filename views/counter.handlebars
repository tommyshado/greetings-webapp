<div class="row">
    {{#each name}}
    <h1>{{username}} number of greetings</h1>

    <table>
        
        <tr>
            <th>Username</th>
            <th>Number of greet</th>
        </tr>

        <tr>
            <td>{{username}}</td>
            <td>{{numberOfGreetings}}</td>
        </tr>

    </table>
    {{/each}}
    
</div>