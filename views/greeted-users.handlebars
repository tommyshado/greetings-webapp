<div class="row">
    <h1>Greeted users data</h1>

    <table>
        
        <tr>
            <th>Username</th>
            <th>Number of greet</th>
        </tr>

        {{#each greetedUsers}}
        <tr>
            <td><a href="/counter/{{name}}">{{name}}</a></td>
            <td>{{count}}</td>
        </tr>
        {{/each}}

    </table>
    
</div>